# Development

Install dependencies and run dev server.

```
> yarn install
> yarn run dev
```

The TypeScript checker is on by default. The linter does not include prettier and needs to be run manually:

```
> yarn run lint
> yarn run lint:fix
```