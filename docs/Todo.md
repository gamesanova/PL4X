# Todo

- Better setup for Bot/AI turns
- The Bot/Human Systems contains effects and is not a "pure" system so may need some reworking.
- This is a strip down of an initial game I setup so likely still needs quite a bit of clean up.
- Load/Save game (to local for now).
- GoTo system for paths (that keeps a target tile on the entity and, and tries to go there on it's own)
- Update initial game init call for setting starting player on game new vs load.
- Sound System.